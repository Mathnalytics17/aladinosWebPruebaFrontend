export const siteConfig = {
  name: "Planner",
  url: "https://planner.tremor.so",
  description: "GlobalOil task project.",
  baseLinks: {
    dashboard:"/dashboard",
  },
}

export type siteConfig = typeof siteConfig
